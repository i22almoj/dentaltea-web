{% extends 'base.html.twig' %}

{% block title %}Secuencias de pictogramas{% endblock %}

{% block body %}
    
<section class="list {% if list.view == 'grid' %}view-grid{% else %}view-table{% endif %} list-lg">
    
    <div class="table-wrapper">
        <div class="table-title mb-4">
            <div class="row">
                <div class="col-sm-12">
                    <h2 class="d-inline-block">Secuencias de pictogramas</h2> <a href="{{ app.request.getSchemeAndHttpHost() }}/admin/secuencias/nueva" class="btn-new btn btn-primary"><i class="material-icons">&#xE147;</i> <span>Añadir secuencia</span></a>
                </div>
                
            </div>
        </div>
            {% for flash_message in app.session.flashbag.get('notice') %}
                <div class="alert alert-success">
                    {{ flash_message }}
                </div>
            {% endfor %} 
        {% include 'admin/sequences/parts/toolbar.html.twig' %}
        
        {% include 'admin/sequences/parts/table.html.twig' %}

        {% include 'admin/sequences/parts/grid.html.twig' %}

        {% include 'admin/sequences/parts/pagination.html.twig' %}
    </div>

</section>

{% endblock %}
